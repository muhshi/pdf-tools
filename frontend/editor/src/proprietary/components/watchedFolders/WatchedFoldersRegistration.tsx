/**
 * Stub for WatchedFoldersRegistration.
 *
 * The real implementation lives in higher layers (saas/desktop). This stub
 * satisfies the import in proprietary/App.tsx so the build succeeds even when
 * the full component tree is not present. WATCHED_FOLDERS_ENABLED is false in
 * this layer, so this component is never rendered at runtime.
 */
export default function WatchedFoldersRegistration() {
  return null;
}
